# feat.md — 优化路线图

> 基于代码现状整理的优化路线图，按类别分组，标注优先级（P0 高 / P1 中 / P2 低）与实施状态。
> ✅ 已完成 / ❌ 不做 / ⏳ 待办

---

## 一、搜索体验

### 1. 升级为 FlexSearch 全文检索 — P0 ✅ 已完成
- **现状**：[search.js](../iris/js/search.js) 使用 FlexSearch 构建标题 + 正文 + 路径复合索引，自定义 CJK 编码器支持中文分词与模糊匹配。

### 2. 搜索结果关键词高亮 — P1 ✅ 已完成
- **现状**：结果 title/preview 中匹配片段用 `<mark>` 标签包裹高亮。

### 3. 搜索范围切换 — P1 ❌ 已移除
- **决策**：用户反馈「区分标题、正文搜索的功能没什么用」，已删除该功能。

### 4. 搜索路径硬编码修复 — P0 ✅ 已完成
- **现状**：直接使用索引中的完整 `path`，移除了 `'docs/' + path` 硬编码拼接。

---

## 二、阅读体验

### 5. 目录树（Index）可折叠与高亮 — ❌ 不做
- **决策**：保持当前扁平列表 + 左边界高亮现状。

### 6. 阅读进度增强 — ❌ 不做
- **决策**：保持仅顶部一条整体进度条。

### 7. 上一篇 / 下一篇导航 — ✅ 已完成
- **现状**：悬浮球菜单提供「上一篇 / 下一篇」按钮，复用 `fileTree.getAdjacentFiles`。

### 8. 面包屑交互增强 — ❌ 不做
- **决策**：保持现状，仅展示路径。

### 8.5 文件树滚动条隐藏 — ✅ 已完成
- **现状**：文件树展开模式下使用 Shadow DOM 隐藏横向滚动条，保留滚动功能。

### 8.6 长表格左右滚动优化 — ✅ 已完成
- **现状**：长表格自动包裹 `.table-wrapper` 支持横向滚动。

---

## 三、主题与外观

### 9. 跟随系统明暗主题 — ❌ 不做
- **决策**：保持手动选择主题。

### 10. 可视化主题自定义（取色器） — P1 ✅ 已完成
- **现状**：设置面板提供强调色 + 中性色取色器，支持亮色/暗色主题定制，保存到 localStorage。

### 10.5 中性色组扩展 — ✅ 已完成
- **现状**：在原有强调色基础上，新增背景/表面/边框/文字/次要文字 5 个中性色取色器，自由度大幅提升。

### 11. 字体/字号/行高可调 — ❌ 不做
- **决策**：保持固定字体样式（可通过自定义 CSS 自行调整）。

---

## 四、性能与加载

### 12. 首屏性能优化 — P1 ✅ 已完成
- **现状**：关键 CSS/脚本 preload、文件树预构建、路由按需加载文档。

### 13. PWA 离线能力增强 — P1 ✅ 已完成
- **现状**：[sw.js](../sw.js) 预缓存首屏静态资源，文档网络优先 + 离线降级缓存，资源更新时 toast 提示用户刷新。

### 14. 构建脚本增量构建 — ❌ 不做
- **决策**：保持每次全量重建。

---

## 五、内容渲染

### 15. 代码块增强 — P1 ✅ 已完成
- **现状**：每个代码块注入复制按钮、语言标签，`data-lang` 属性便于扩展，长行横向滚动。

### 16. 图片灯箱 — P2 ✅ 已完成
- **现状**：点击图片打开全屏灯箱，支持滚轮缩放、左右键翻页、Esc 关闭，自动收集当前文档所有图片支持画廊模式。

---

## 六、可访问性与交互

### 17. 可访问性（a11y）系统补齐 — ❌ 不做
- **决策**：暂不处理，后续再说。

### 18. 全局快捷键 — ❌ 不做
- **决策**：仅保留 Esc 关闭侧栏/灯箱。

### 18.5 悬浮球菜单扩展 — ✅ 已完成
- **现状**：从 4 个入口扩展到 7 个：回到顶部、上一篇、下一篇、打开本地 MD、安装 PWA、编辑此页、设置。

### 18.6 打开本地 MD — ✅ 已完成
- **现状**：悬浮菜单提供「打开本地 MD」按钮，使用 FileReader 读取本地文件预览（不写入 URL，刷新后丢失）。

---

## 七、SEO 与分享

### 19. 动态 SEO meta 与 Open Graph — P2 ✅ 部分完成
- **现状**：每篇文档动态更新 `document.title`（frontmatter.title 优先，其次首个 `# 标题`）。
- **待补全**：OG meta（`og:title`/`og:description`/`og:url`）目前仍是静态的，未来可扩展为动态。

### 20. 标题锚点分享（复制直达链接） — P2 ✅ 已完成
- **现状**：标题悬浮出现「复制链接」按钮，分享直达章节。

---

## 八、输出与订阅

### 21. 导出 PDF — ✅ 已完成
- **现状**：设置面板 → 操作 → 导出 PDF，通过 `window.print()` + `@media print` 样式隐藏 UI 元素。

### 22. RSS 源 — ✅ 已完成
- **现状**：[build-feed.js](../iris/scripts/build-feed.js) 扫描 docs/ 生成 RSS 2.0 feed.xml，GitHub Actions 自动重建，index.html 添加 `<link rel="alternate">` 支持自动发现。详见 [RSS 订阅](rss.md)。

### 22.5 自定义高亮 JS — ✅ 已完成
- **现状**：设置面板 → 高级 → 自定义高亮 JS，加载外部 highlight.js 主题 CSS URL，回车应用。

---

## 八.5、内置 Markdown 编辑器

### 22.6 类 Jupyter Cell 编辑器 — ✅ 已完成
- **现状**：[editor.js](../iris/js/editor.js) + [editor.css](../iris/css/editor.css) 实现全屏覆盖层编辑器，按 Cell 编写并即时渲染，复用 `markdown.js` 渲染管线。入口：URL `?mode=editor` / 设置面板「打开编辑器」/ `MarkdownPreview.enterEditorMode()`。详见 [编辑器说明](editor.md)。

### 22.7 自动保存 — ✅ 已完成
- **现状**：localStorage `mdnb_autosave_v2`，1.5s 防抖写入，序列化所有 Cell 的 id/type/content/output_html，刷新后自动恢复。

### 22.8 自动补全系统 — ✅ 已完成
- **现状**：11 类触发字符（`@` / ` ``` ` / `> [!` / `#` / `-` / `|` / `---` / `>` / `$$` / `![` / `[`），70+ 条目，↑↓ 选择、Enter/Tab 确认、Esc 关闭。

### 22.9 搜索替换 — ✅ 已完成
- **现状**：跨所有 Cell textarea 查找 / 替换 / 全部替换，`F3` / `Shift+F3` 导航，状态栏显示匹配数与当前位置。

### 22.10 工具栏菜单与私有语法 — ✅ 已完成
- **现状**：8 大下拉菜单（Markdown / HTML / 私有语法 / 工具渲染 / 插入 / 嵌入 / 下载 / 导入），覆盖 17 种画廊样式、5 种 GitHub Alerts、Mermaid 15 种、PlantUML 12 种、ApexCharts 13 种、13 种外部嵌入、17 种原生 HTML 标签等。

### 22.11 右键菜单与 Cell 操作 — ✅ 已完成
- **现状**：2 列 17 项右键菜单，左列「运行与插入」7 项，右列「编辑与清理」10 项，`showContextMenu` 四方向边界检测定位，窄屏回退单列。

### 22.12 导入导出 — ✅ 已完成
- **现状**：支持 `.md` / `.html` / 内联 CSS HTML / `.pdf` / `.mdnb` 笔记本 / 合并 `.md` 导出，`.mdnb` / `.md`（按 `---` 切分）/ `.md` 到当前 Cell 导入。

### 22.13 SVG 图标库 — ✅ 已完成
- **现状**：index.html 顶部内嵌 24 个 SVG symbol 的图标精灵，全站 UI（含编辑器）统一使用 `<svg class="ico"><use href="#i-xxx"/></svg>` 引用，已完全移除 emoji。

---

## 八.6、Packet Tracer 拓扑渲染

### 22.14 .pkt 文件解密解析管线 — ✅ 已完成
- **现状**：[iris/scripts/pkt/](../iris/scripts/pkt/) 下 5 个 Python 脚本（零第三方依赖）实现完整管线：
  - `decrypt.py` — XOR（旧版）+ Twofish EAX（PT 7.3+）解密，含纯 Python Twofish 实现
  - `decompress.py` — zlib 多策略解压（size-prefixed / raw / deflate / gzip 兜底）
  - `parse.py` — XML 解析 + Cisco IOS 配置提取（接口 / VLAN / ACL / 路由）
  - `output.py` — 分区化 JSON schema（`{meta, devices, links, configs, interfaces, vlans, acls, routes, groups}`）
  - `main.py` — mtime 增量构建 + 错误兜底 JSON

### 22.15 Cytoscape.js 拓扑渲染 — ✅ 已完成
- **现状**：[pkt-renderer.js](../iris/js/pkt/pkt-renderer.js) 基于 Cytoscape.js 渲染交互式拓扑图，`@[pkt](name)` 嵌入语法集成到 [embedded.js](../iris/js/renderers/embedded.js)。功能包括：
  - 13 种设备类型 SVG 图标 + 线缆类型颜色映射
  - 节点点击弹出右侧抽屉（5 标签页：接口表 / 配置 / VLAN / ACL / 路由）
  - 搜索高亮（按设备名 / IP / 类型过滤）+ 布局切换（PT 坐标 ↔ 力导向）+ 网格背景
  - 导出 PNG / JSON / Markdown
  - 自定义 IOS 语法高亮 + 移动端响应式（抽屉底部弹出）
- **CI**：[build-pkt.yml](../.github/workflows/build-pkt.yml) 在 `iris/data/pkt/raw/` 或 `scripts/pkt/` 变更时自动触发

---

## 九、工程与质量

### 23. 核心模块单元测试 — ❌ 不做
- **决策**：暂不需要测试。

### 24. 界面国际化（i18n） — ❌ 不做
- **决策**：保持纯中文界面。

### 25. 静态评论系统 — ❌ 不做
- **决策**：保持纯阅读站点。

### 26. 调试日志清理 — ❌ 不做
- **决策**：保留现有 console.log。

---

## 实施记录

### 第一批（最易改动）
1. ✅ #4 搜索路径硬编码修复
2. ✅ #2 搜索结果关键词高亮
3. ✅ #20 标题锚点分享

### 第二批（核心优化）
1. ✅ #1 FlexSearch 全文检索
2. ✅ #12 首屏性能优化
3. ✅ #13 PWA 离线能力增强

### 第三批（主题与渲染）
1. ✅ #10 可视化主题自定义（取色器）
2. ✅ #10.5 中性色组扩展
3. ✅ #15 代码块增强（复制 + 语言标签）
4. ✅ #16 图片灯箱

### 第四批（输出与订阅）
1. ✅ #21 导出 PDF
2. ✅ #22.5 自定义高亮 JS
3. ✅ #22 RSS 源

### 第五批（交互优化）
1. ✅ #8.5 文件树滚动条隐藏
2. ✅ #8.6 长表格左右滚动优化
3. ✅ #7 上一篇 / 下一篇导航
4. ✅ #18.5 悬浮球菜单扩展到 7 个入口
5. ✅ #18.6 打开本地 MD
6. ✅ #3 移除搜索范围切换（用户反馈无用）

### 第六批（内置编辑器）
1. ✅ #22.6 类 Jupyter Cell 编辑器（全屏覆盖层 + Cell 模型 + 运行渲染）
2. ✅ #22.7 自动保存（localStorage 防抖）
3. ✅ #22.8 自动补全系统（11 类触发字符，70+ 条目）
4. ✅ #22.9 搜索替换（跨 Cell 查找 / 替换 / F3 导航）
5. ✅ #22.10 工具栏菜单与私有语法（7 大下拉）
6. ✅ #22.11 右键菜单与 Cell 操作（2 列 17 项）
7. ✅ #22.12 导入导出（.md / .html / .pdf / .mdnb）
8. ✅ #22.13 SVG 图标库（24 symbol，全站移除 emoji）

### 第七批（Packet Tracer 拓扑渲染）
1. ✅ #22.14 .pkt 文件解密解析管线（Python 零依赖：XOR + Twofish EAX + zlib + XML + IOS 配置解析）
2. ✅ #22.15 Cytoscape.js 拓扑渲染（@[pkt] 嵌入 + 抽屉 5 标签页 + 搜索高亮 + 导出 + CI 自动构建）

---

## 后续可能方向

- 动态 OG meta（社交分享卡片）
- 跟随系统明暗主题
- a11y 系统补齐
- 全局快捷键
